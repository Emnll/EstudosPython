#Usando uma biblioteca se utiliza a função trunc
#Using a library the function trunc is tested

from math import trunc
num = float(input('Digite um número quebrado (usando ponto): '))
print('O número inteiro é {}'.format(trunc(num)))
