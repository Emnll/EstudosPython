#Usando shuffle para sortear um nome
#Using shuffle to sort a name
import random
nome = ['Alberto','Ana','Luiza','Gustavo']
random.shuffle(nome)
print('A ordem de apresentação será: {}.'.format(nome))
