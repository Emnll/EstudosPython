#testing import and using sqrt
#testando import e usando sqrt para calcular a raiz quadrada
from math import sqrt
num = int(input('Digite um Número: '))
raiz = sqrt(num)
print('A raiz de {} é igual a {}'.format(num,raiz))
